name: Foundry Unit Tests

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main

jobs:
  unit:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
    name: Forge unit tests (${{ matrix.package }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package: [bridge, core, governance, safetymodule, staking, vault]
    defaults:
      run:
        working-directory: contracts
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Foundry CI
        uses: ./.github/actions/setup-foundry-ci
        with:
          workdir: contracts
          cache-prefix: forge-out-unit-${{ matrix.package }}
      - name: Forge unit tests
        run: forge test --match-path "test/${{ matrix.package }}/**/*.t.sol" --no-match-path "test/${{ matrix.package }}/**/*.{integration,invariant,e2e}.t.sol"
        env:
          FOUNDRY_PROFILE: ci
