# Phase 4 — Update Component Specs

Scope
- Update files under `research/technical/architecture/components/*.md`.

Edits
- For each component, align:
  - Purpose, state variables, events, key functions, and role gates.
- Remove stale fields (e.g., treasury fields/hooks if not in end-state), or clearly mark as "planned" if end-state includes them.

Acceptance
- Each component spec reflects the same end-state as `flows.md`.
- No contradictions between component specs and `interfaces-and-roles.md`.
