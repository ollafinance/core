# Phase 6 — Repo-Wide Consistency Sweep

Scope
- Sweep `research/**` for outdated names and mismatched flows.

Edits
- Replace stale identifiers (role names, contract names, function names).
- Ensure references point to the correct docs under `research/technical/architecture/**`.
- Validate mermaid blocks across edited files.

Acceptance
- Grep for known stale terms returns no hits (or only in "historical" sections).
- Docs form a consistent narrative across overview, flows, components, roles, invariants.
