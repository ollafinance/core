---
description: Run lint:fix then lint, summarize issues
agent: smart-contract-dev
---

Run `yarn lint:fix`, then `yarn lint`.
Summarize any remaining issues and propose minimal fixes.

!`yarn lint:fix`
!`yarn lint`
