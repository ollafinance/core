---
description: Run lint:fix then lint, fix everything
agent: smart-contract-dev
---

Run `yarn lint:fix`, then `yarn lint`.
If lint fails, fix everything you can and re-run until it passes, or explain what is blocking.

!`yarn lint:fix`
!`yarn lint`
